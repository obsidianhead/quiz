// Re-export functions from quiz-ui.js for testing only
export const fetchCourses = window.fetchCourses;
export const createCourseSelection = window.createCourseSelection;
export const loadCourseConfig = window.loadCourseConfig;
export const createChapterAndTestSelection = window.createChapterAndTestSelection;
export const startQuiz = window.startQuiz;
export const shuffleArray = window.shuffleArray;
export const startQuizTimer = window.startQuizTimer;
export const showQuizTimer = window.showQuizTimer;
export const setNextQuestion = window.setNextQuestion;
export const resetState = window.resetState;
export const showQuestion = window.showQuestion;
export const submitAnswer = window.submitAnswer;
export const nextQuestion = window.nextQuestion;
export const endQuiz = window.endQuiz;
